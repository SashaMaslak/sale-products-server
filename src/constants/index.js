const {
  dateRegex,
  onlyLettersRegex,
  cityRegex,
  emailRegex,
  pswRegex,
  phoneRegex,
} = require("./regex")

module.exports = {
  dateRegex,
  onlyLettersRegex,
  cityRegex,
  emailRegex,
  pswRegex,
  phoneRegex,
}
